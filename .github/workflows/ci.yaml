name: ci 
on: [push]

jobs:
  build:
    name: test-local-vm
    runs-on: debian:buster
    steps:
       - name: Checkout
         uses: actions/checkout@v1`

       - name: bootstrap-local
         run: ./bootstrap/bootstrap-local

