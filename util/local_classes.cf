bundle common local_classes {
  vars:
    "class_file" string => "/usr/local/etc/cfengine/classes.txt";
    "class_strs"  slist  => readstringlist("$(class_file)"
                                         , "#.*$"
					 , "\n"
					 , inf
					 , inf);

  classes:
    "$(class_strs)" expression => "any";

  reports:
    "class set: $(class_strs)";

    "hello!!";
}
